import { h } from 'preact';
import './Header.scss';

const Header = () => (
	<header class="app-header" role="banner">
		<h1 class="app-header__title">
            WashUp
        </h1>
	</header>
);

export {Header};
